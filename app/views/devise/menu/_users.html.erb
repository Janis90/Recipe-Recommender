<% if user_signed_in? %>
    <% if current_user.admin? %>
        <li class="<%= is_active?(users_path)%>">
          <%= link_to( t('header.users'), users_path) %>
        </li>
    <%end %>
<% end %>